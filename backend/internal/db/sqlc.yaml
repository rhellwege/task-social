version: "2"
sql:
  - engine: "sqlite"
    schema: "sql/schemas/"
    queries: "sql/queries/"
    gen:
      go:
        package: "repository"
        out: "repository"
        json_tags_id_uppercase: true
        emit_json_tags: true
        emit_interface: true
        emit_pointers_for_null_types: true
